{
  "name": "job-radar",
  "private": true,
  "workspaces": ["backend", "client"],
  "scripts": {
    "api": "npm run start:dev --workspace=backend",
    "ui": "npm run dev --workspace=client",
    "build": "npm run build --workspace=backend && npm run build --workspace=client",
    "test": "npm run test --workspace=backend"
  }
}
